# Прямое наследование от полного dim10.yaml намеренно:
# файл корректен и с новым рекурсивным загрузчиком, и со старым одноуровневым.
_base: "dim10.yaml"

experiment:
  name: "dim50_eps45"
  dim: 50
  device: "auto"

metrics:
  eps_hit: 4.5
